# ModelForge 实现路径规划

> **计划修订 (2026-06-06)**：本文已按当前代码进度重新校准。勾选 `[x]` 仅表示“仓库中已有可运行实现并有基本验证”；如条目目标包含“完整/生产/导出/SSO/100+”等未落地能力，或仅有辅助代码但尚未接入 API / 命令 / Ribbon 用户路径，则保持未勾选，并在括号中标明已完成的基础部分。

## 1. 实施原则

### 1.0 当前项目判断

当前仓库已通过 **Phase A 开发验证**，`src/`、`manifest/`、测试工程和 CI 配置均已建立。源码骨架包含 Sidecar、Backend、Web Add-in 和共享 Contracts。近期工作应聚焦于已落地功能的产品化、测试覆盖提升和安装包真机回归：

1. 明确 MVP 边界和阶段验收标准。
2. 固化 Sidecar / Web Add-in / 后端桥接的技术边界。
3. 建立项目骨架、命令目录、接口契约和测试基线。
4. 通过 Excel Sidecar MVP 验证核心产品价值，再扩展到 PowerPoint、Word、AIWA 和企业后台。

### 1.1 优先级策略

1. **核心功能优先**: Excel 财务建模工具优先 (P0)。
2. **高频功能优先**: 快捷键、跨应用联动、Deck Check 等。
3. **可独立运行**: 每个模块可独立测试和发布。
4. **平滑升级**: 兼容已有代码，便于后续扩展。

### 1.2 开发模式

- 阶段式开发，每个阶段交付可用功能。
- 单元测试覆盖关键功能。
- 持续集成确保代码质量。

## 2. 技术路线

### 2.1 架构设计：Sidecar + Office JS API 混合架构 + 后端桥接

#### 2.1.1 架构选型理由

| 能力需求                                        | Web Add-in  | Sidecar (.NET 10 + 原生 COM) | 结论                       |
| ----------------------------------------------- | ----------- | ---------------------------- | -------------------------- |
| Keyboard Shortcuts (快捷键)                     | 部分支持    | ✅ Win32 全局钩子            | **P0级刚需，Sidecar 主责** |
| Link Excel to PowerPoint/Word (深度COM引用绑定) | ❌ 能力有限 | ✅ 原生 COM 可支持           | Sidecar 实现               |
| 颜色板、共享库检索                              | ✅ 轻量更新 | ❌ 需重新安装                | Web 更灵活                 |
| Deck Check 报告生成界面                         | ✅ 热更新   | ❌ 需重新安装                | Web 更优                   |
| Ribbon 菜单融合                                 | ✅ 支持     | N/A (通过 manifest 驱动)     | Web Add-in 承载主要 Ribbon |

**重要澄清**：Sidecar 与 Web Add-in 运行在不同进程，禁止直接通信。两者的功能协调必须通过**后端 API 桥接** (ASP.NET Core 10) 和 Sidecar localhost REST API 实现。

#### 2.1.2 功能分工图（2026 重构版）

```text
┌─────────────────────────────────────────────────────────────────┐
│                    Ribbon 功能区（统一入口）                      │
├─────────────────────────────┬───────────────────────────────────┤
│   Sidecar (.NET 10 + 原生 COM)│        Web Add-in Commands        │
│   COM 深度操作 + 快捷键       │      管理、配置、AI 与可视化体验      │
├─────────────────────────────┴───────────────────────────────────┤
│ Sidecar 负责：                      Web Add-in 负责：              │
│ ├── 全局键盘钩子 (Win32)            ├── Admin Console             │
│ ├── 原生 COM 互操作            ├── Shared Libraries          │
│ ├── 公式追踪 + Model Check          ├── Corporate Dictionary      │
│ ├── 文件优化 + Prepare to Share     ├── Dashboard 统计            │
│ ├── Power Tools                     ├── Omnibar 速查栏            │
│ ├── Visualizations                  ├── AIWA 前端 UI              │
│ ├── 高级图表 Chart Add-ons          └── 企业策略下发              │
│ └── PPT/Word 底层交互                                             │
│                                                                   │
│ Backend 负责：认证、配置、审计、链接元数据、字典、命令桥接、遥测     │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.1.3 技术栈

| 层级               | 技术选型                                                 | 说明                                     |
| ------------------ | -------------------------------------------------------- | ---------------------------------------- |
| **Sidecar 运行时** | .NET 10 Minimal API                                      | COM 操作 + 快捷键                        |
| **COM 互操作**     | `oleaut32!GetActiveObject` P/Invoke + `dynamic` 延迟绑定 | 零 PIA，按需连接运行中的 Office COM 实例 |
| **Office JS API**  | Office.js + Office Add-in Manifest                       | Web 插件                                 |
| **后端桥接 API**   | ASP.NET Core 10 (REST + SignalR)                         | 跨进程通信                               |
| **前端框架**       | React 18 / TypeScript                                    | Web Add-in UI                            |
| **UI 组件库**      | Fluent UI React                                          | Microsoft 设计系统                       |
| **构建工具**       | Vite                                                     | Web Add-in 打包                          |
| **数据库**         | InMemory / SQLite / PostgreSQL                           | 配置、审计、链接元数据                   |

#### 2.1.4 项目结构

```text
ModelForge/
├── src/
│   ├── sidecar/                       # Sidecar Web Host (.NET 10 Minimal API + 原生 COM)
│   │   └── ModelForge.Sidecar/
│   │       ├── Program.cs             # Host + DI + REST (:5200)
│   │       ├── Commands/              # 命令 ID + 快捷键注册
│   │       ├── Configuration/         # BridgeOptions
│   │       ├── Interop/               # 原生 COM 互操作
│   │       ├── Keyboard/              # Win32 键盘钩子
│   │       ├── Api/                   # Sidecar REST 端点
│   │       └── Services/              # BackendBridgeClient
│   ├── web/                           # Web Add-in 项目
│   │   ├── src/                       # React + TypeScript
│   │   ├── package.json
│   │   └── vite.config.ts
│   ├── backend/                       # 后端 API 桥接 (ASP.NET Core 10)
│   │   ├── Controllers/
│   │   ├── Services/
│   │   ├── Data/
│   │   └── Program.cs
│   ├── shared/                        # 共享类型/常量 (.NET 10)
├── manifest/                          # Add-in Manifest
└── docs/
```

### 2.2 开发实施风险与对策

| 风险                                                   | 影响 | 缓解措施                                                                                         |
| ------------------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------ |
| Sidecar 与 Web Add-in 进程隔离                         | 高   | 强制使用 Sidecar localhost REST + 后端 API 桥接                                                  |
| Sidecar 与 Office 进程权限不匹配或 ROT 指向 WPS/旧 COM | 中   | 原生 COM 内建重试 + 诊断日志；运行时校验 Microsoft Office 版本/路径，拒绝 WPS/Office 12 兼容对象 |
| .NET 运行时依赖                                        | 中   | Self-contained 部署，无外部运行时依赖                                                            |
| 快捷键冲突                                             | 低   | 计划提供冲突检测 UI，允许用户自定义                                                              |

## 3. 实施阶段

### 阶段一：基础设施建设 (预计周期 2 周)

#### 目标

搭建 Sidecar (.NET 10 + 原生 COM) + Web Add-in + 后端桥接 (.NET 10) 混合架构项目框架。

#### 任务清单

##### 1.1 Sidecar 项目搭建

- [x] 1.1.1 创建 .NET 10 Web Host (Minimal API) 项目 (`src/sidecar/ModelForge.Sidecar/`)
- [x] 1.1.2 集成原生 COM (oleaut32!GetActiveObject P/Invoke + dynamic)
- [x] 1.1.3 创建命令常量、快捷键定义、后端客户端（复用原命令模型）
- [x] 1.1.4 搭建 Win32 全局键盘钩子框架 (WH_KEYBOARD_LL)
- [x] 1.1.5 实现 Sidecar 本地 REST API 端点 (port 5200)
- [ ] 1.1.6 搭建 Serilog 日志和异常处理框架（当前为 `ILogger` 基础日志 + Sidecar `/api/execute` / `/api/status` 结构化信封 + Backend `/api/admin/diagnostics` 只读诊断摘要 + `/api/admin/diagnostics/bundle` 安全 JSON 诊断包；Serilog、真实日志文件打包和全端点 TraceId 贯通待接入）

##### 1.2 Web Add-in 项目搭建

- [x] 1.2.1 创建 React + TypeScript Web Add-in 项目 (Vite + Office.js)
- [x] 1.2.2 配置 Fluent UI React 和导航面板
- [x] 1.2.3 实现侧边栏任务窗格 (Task Pane) 与多面板路由
- [x] 1.2.4 添加 `@types/office-js` 类型定义

##### 1.3 后端桥接 API 搭建

- [x] 1.3.1 ASP.NET Core 10 Minimal API 项目 (目标 `net10.0`)
- [x] 1.3.2 实现用户认证基架 (JWT / RBAC roles)
- [ ] 1.3.3 设计数据库 Schema (用户、配置、日志、字典) — 配置/审计/链接/字典已建 EF Core Schema；用户仍为内存存储，完整用户 Schema 待补
- [x] 1.3.4 实现 Sidecar 命令分发、审计事件、配置管理 REST 端点

##### 1.4 集成与通信测试

- [x] 1.4.1 Sidecar 通过 HTTP 调用后端 API
- [x] 1.4.2 Web Add-in 通过 HTTP 调用后端 API
- [x] 1.4.3 Web Add-in 通过 localhost REST 调用 Sidecar
- [x] 1.4.4 制定接口契约文档（`docs/API契约.md`）
- [x] 1.4.5 建立 CI（Backend Test + Sidecar Test + Solution Build + Web Build）

#### 交付物

- 可运行的 Sidecar REST 服务 (localhost:5200)
- 可运行的 Web Add-in (侧边栏显示 "Hello World")
- 后端 API 服务 (支持健康检查和基本配置读写，升级到 .NET 10)
- Ribbon 融合原型 (至少各一个菜单项)
- `docs/技术路线规划.md` 终稿
- `docs/API契约.md` 更新
- `docs/开发环境搭建.md` 更新

#### 阶段门禁

- Sidecar 可在本机启动并提供 `/health` 接口。
- Web Add-in 可在 Office 中侧载并显示 ModelForge Ribbon。
- Sidecar、Web Add-in、Backend 三条通信链路均可达。
- 关键架构决策记录在 `docs/技术路线规划.md`，包括 Sidecar、原生 COM、数据库、安装方式。

### 阶段二：Sidecar + Web Add-in 核心功能 (预计周期 4-5 周)

#### 目标

推进 Sidecar + Web Add-in 中 P0 级 Excel 功能；当前为基础版，仍有 100+ 快捷键对应命令规格、图表工具、失效链接自动修复等产品化缺口。

#### 任务清单

##### 2.1 Power Tools (建模自动化套件)

- [x] 2.1.1 快速向右/向下填充 (Fast Fill Right/Down)
- [x] 2.1.2 错误封装 (一键添加 IFERROR 包裹)
- [ ] 2.1.3 公式简化助手 (调用后端 API 分析公式并给出简化建议)
- [x] 2.1.4 描述性统计插入 (MIN/MAX/AVERAGE/COUNT/SUM 一键生成)

##### 2.2 快捷键系统 (完整版)

- [ ] 2.2.1 支持 100+ 快捷键的自定义映射表（当前 39 个已实现 Excel/PPT/Word 命令默认映射、Sidecar 导入/导出和 Web 配置界面已完成；继续扩展到 100+ 需要先定义并实现对应命令/用户路径，不能只追加无效映射）
- [x] 2.2.2 快捷键配置界面 (对话框，可导入导出)（Excel 面板内基础配置 UI 已完成，支持刷新、编辑、保存、JSON 导入/导出和冲突回滚）
- [x] 2.2.3 全局钩子实现，不影响原生快捷键（基础版）

##### 2.3 Visualizations (视觉审计)

- [x] 2.3.1 扫描活动工作表，识别硬编码 (无公式)、公式、外部链接
- [x] 2.3.2 分别应用三色填充 (蓝/黑/绿)
- [x] 2.3.3 提供一键清除审计配色

##### 2.4 Prepopulated Tables (金融模板库)

- [x] 2.4.1 实现 DCF 估值表模板 (当前为内置基础模板)
- [ ] 2.4.2 实现 Black-Scholes 定价表模板
- [ ] 2.4.3 允许用户将选定区域另存为个人模板

##### 2.5 跨应用数据联动 (基础版)

- [ ] 2.5.1 Excel → PowerPoint: 将选定的 Range 或 Chart 导出到 PPT 指定位置，建立链接（Range 命令基础版已接入；Chart 链接 helper 存在但尚未接入命令/Ribbon）
- [ ] 2.5.2 Excel → Word: 类似功能（`word.embed-excel-range` 基础版已接入；Excel 侧入口和后端 LinkMetadata 记录待补）
- [x] 2.5.3 刷新机制: 后端存储链接元数据，用户点击"刷新"时 Sidecar 读取并执行更新（基础版已完成：`excel.refresh-links` 读取 Backend `/api/links`，可按 `targetAddress` 精准定位 PowerPoint `SlideN/ShapeN|ChartN|Name` 和 Word `FieldN|InlineShapeN|TableN` 目标刷新，并保留本机全量刷新回退；失效链接自动修复待补）

##### 2.6 高级图表与 Chart 工具

- [ ] 2.6.1 向已有图表添加数据序列 (通过对话框选择新数据区域)
- [ ] 2.6.2 批量图表格式化 (统一大小、字体、颜色主题)
- [ ] 2.6.3 快速插入柱状图/折线图 (Quick Chart)

##### 2.7 文件优化与 Prepare to Share

- [ ] 2.7.1 Workbook Optimization: 清除未使用样式、无效名称、外部链接残留（无效名称/外部链接残留基础清理已实现；未使用样式清理待补）
- [ ] 2.7.2 Prepare to Share: 一键将所有公式转为数值，移除所有批注，可选脱敏（安全副本、公式转值、批注/元数据清理、隐藏行列和外链处理已实现；可选脱敏待补）

##### 2.8 Model Check 引擎

- [ ] 2.8.1 扫描工作簿，列出所有硬编码值的位置（当前 Model Check 只扫描活动工作表；全工作簿扫描待补）
- [ ] 2.8.2 检测并报告所有外部链接 (跨工作簿)（当前 Model Check 只扫描活动工作表公式；全工作簿/LinkSources 汇总待补）
- [x] 2.8.3 检测公式错误值 (#REF!, #N/A, #VALUE!)
- [ ] 2.8.4 检测循环引用，提供高亮和修复建议（循环引用检测/报告基础版已实现；高亮和修复建议待补）

##### 2.9 公式追踪 (基础版)

- [ ] 2.9.1 高亮直接前驱（PrecedentTracer） (Precedents)（直接前驱列表已返回；可视化高亮/箭头待补）
- [ ] 2.9.2 高亮直接依赖（DependentTracer） (Dependents)（直接依赖列表已返回；可视化高亮/箭头待补）
- [ ] 2.9.3 跨工作表追踪（当前仅基础 DirectPrecedents/DirectDependents；跨工作表可视化追踪待补）

##### 2.10 工作簿管理与格式工具

- [ ] 2.10.1 批量取消隐藏/重命名工作表
- [x] 2.10.2 Names Manager（扫描/删除逻辑、Web 面板和 `excel.names-manager` Sidecar 命令路由已接入）
- [x] 2.10.3 财务格式快速应用（ApplyFinanceFormat） (会计格式、千分位、百分比)

#### 交付物

- Excel Sidecar 基础版（Power Tools、Visualizations、Model Check 部分能力、Prepare to Share 部分能力、链接原型等核心命令已落地）
- 快捷键框架与 39 个已实现命令映射；配置界面和 JSON 导入/导出已实现，100+ 需要新增真实命令规格后再推进
- Excel → PPT/Word 跨应用联动基础演示；后端元数据驱动刷新和单个对象精准定位基础版已实现，Excel 侧 Word 入口、PPT Chart 链接命令和失效链接修复待补
- 单元/集成/COM 测试覆盖关键路径

#### 阶段门禁

- 当前 39 个已实现命令默认快捷键已注册；用户可在 Web Excel 面板编辑并导入/导出 JSON；100+ 全量产品化映射需先补命令规格，企业策略下发仍待补。
- Model Check 活动工作表路径覆盖错误值、外部链接、循环引用、硬编码；全工作簿扫描、无效名称、隐藏工作表/隐藏行列仍未并入 Model Check 报告。
- Prepare to Share 默认生成副本，不覆盖源文件；可选脱敏策略待实现。
- Excel → PowerPoint/Word 链接可创建；刷新已能读取后端 LinkMetadata 并按目标对象精准定位，目标地址不完整时回退 Office OLE/字段全量扫描；失效链接修复待补。
- 大范围扫描进度提示和取消能力待实现。

### 阶段三：Web Add-in 核心功能 + 后端深度集成 (预计周期 3 周，技术栈不变)

#### 目标

建设企业后台管理、Dashboard、Omnibar、AIWA 前端和 Corporate Dictionary 集成；当前为基础版，生产 LLM、Dashboard 统计和完整 Admin 能力待补。

#### 任务清单

##### 3.1 Admin Console (企业后台)

- [ ] 3.1.1 用户管理页面 (列表、增删改查，对接 SSO 模拟)（当前支持列表、新增、启停，并已补 Admin 用户生命周期 E2E；编辑/删除/SSO 模拟待补）
- [ ] 3.1.2 角色权限配置 (RBAC)（Backend JWT/RBAC 基础和 `/api/admin/roles` 只读角色权限矩阵已实现；Admin Console 已展示内置角色/权限；自定义角色、权限编辑和用户组映射待补）
- [x] 3.1.3 审计日志查看与导出（Admin Console 可查看/筛选，Backend 支持 CSV 导出，前端提供导出按钮；审计页支持保留策略 dry-run/执行清理；诊断面板已接入 `/api/admin/diagnostics` 和诊断包下载）
- [ ] 3.1.4 企业策略配置 (例如强制快捷键配置、禁用某些功能)

##### 3.2 Shared Libraries & Corporate Dictionary

- [ ] 3.2.1 品牌模板管理 (上传 Logo、配色方案、字体文件)
- [ ] 3.2.2 合同条款库 (文本片段管理)
- [x] 3.2.3 Corporate Dictionary 管理界面 (支持正则表达式、替换规则、命中高亮)（术语增删、正则、替换规则、样例检查、命中高亮 UI、JSON 批量导入/导出和 CSV/XLSX 模板导入/导出已完成）

##### 3.3 Dashboard 统计

- [x] 3.3.1 收集客户端使用事件 (通过后端 API 上报)（基础版已完成：审计事件 API、`/api/admin/audit-events/summary` 聚合摘要、认证 `auth.*` 审计、Sidecar `/api/execute` 成功/校验失败/Office 未运行/COM 异常路径自动上报，Backend Admin 用户/配置/字典写操作 `admin.*` 审计，以及 Web Add-in 导航/命令/快捷键/DeckCheck/链接/Admin/词典/AIWA 显式点击 `ui.*` best-effort 上报；更细粒度输入变更和页面停留分析待补）
- [x] 3.3.2 热力图展示高频功能（基础版已完成：`/api/admin/audit-events/summary` 返回事件类型 × 宿主 heatmap，Admin Console 审计页展示 Top 事件热力图，并支持事件类型/用户/宿主摘要和热力图单元点击下钻；多维自定义分组待补）
- [ ] 3.3.3 趋势图表 (按天/周/月)

##### 3.4 Omnibar 速查栏

- [ ] 3.4.1 全局搜索框 (可搜索 Office 原生命令、ModelForge 扩展命令)（当前仅搜索后端 ModelForge 命令目录；Office 原生命令待补）
- [ ] 3.4.2 根据当前宿主程序 (Excel/PPT/Word) 动态过滤命令（当前显示宿主标签并可按命令搜索；自动识别当前宿主过滤待补）
- [ ] 3.4.3 历史记录存储 (本地 IndexedDB)

##### 3.5 AIWA 前端 UI + 后端调用

- [x] 3.5.1 对话界面 Chat UI（AiwaChat 组件，5 模式 Mock 交互）
- [ ] 3.5.2 后端集成大模型 API（当前仅 Ollama Docker Compose 配置 + 前端 Mock；Backend LLM 调用端点未实现）
- [x] 3.5.3 自动注入 Corporate Dictionary（AIWA Mock 响应后处理已调用 `/api/dictionary/check` 并展示命中/替换建议；生产 LLM 链路待接入）

##### 3.6 配置同步

- [ ] 3.6.1 用户偏好设置页面 (快捷键、默认模板)
- [ ] 3.6.2 云端同步 (可选，通过 OneDrive 或后端存储)

#### 交付物

- Web Add-in 管理面板基础版（React + ASP.NET Core API）
- Corporate Dictionary 支持正则校验、增删、样例文本检查、命中高亮、JSON 批量导入/导出和 CSV/XLSX 模板导入/导出
- Omnibar 可搜索后端命令目录；历史记录待实现
- AIWA Mock 对话功能 demo，已接入词典后处理；生产 LLM 后端待实现

#### 阶段门禁

- Admin Console 可管理术语库、批量导入/导出 JSON/CSV/XLSX 并高亮检查命中；用户管理、内置角色权限只读页、审计摘要 Top 10、审计趋势条形图、审计热力图、摘要/热力图点击下钻和审计保留策略已完成基础版，角色编辑、用户组、时间趋势区间下钻和策略配置仍需产品化。
- Corporate Dictionary 可被 AIWA Mock 调用；Deck Check 已支持手工 `forbiddenTerms` 参数，并可在配置服务令牌后自动读取 Backend 字典；幻灯片内术语高亮仍待补。
- 审计日志可查看，支持按事件类型、用户、宿主、级别和关键词做服务端筛选，同条件 CSV 导出，以及 `AuditRetentionDays` 保留策略预览/清理。
- AIWA 仅在用户显式触发时处理内容，并支持关闭或模拟模式。

### 阶段四：PowerPoint 和 Word Sidecar 功能 (预计周期 3-4 周)

#### 目标

通过 Sidecar + 原生 COM 实现 PowerPoint 和 Word 中 P0-P1 级 COM 操作基础能力。Web Add-in manifest 扩展 PPT/Word 宿主支持。

#### 任务清单

##### 4.1 PowerPoint 高级功能

- [x] 4.1.1 **Dynamic Agendas**: 读取 PPT 的 Sections，动态生成/更新目录页
- [ ] 4.1.2 **Pagination**: 实现对开双页、白页标注、自定义页码格式
- [ ] 4.1.3 **New Presentation from Template**: 基于预留模板 (.potx) 创建新文件
- [ ] 4.1.4 **TurboShapes 基础库**: 实现 Harvey Balls、Progress Bar、Rating Stars 至少三种
- [ ] 4.1.5 **MasterShapes**: 将选中的形状保存为库，支持拖拽插入
- [ ] 4.1.6 **Meta Shapes**: 为形状附加键值对元数据，支持按元数据筛选
- [ ] 4.1.7 **Logo Library**: 集成 Brandfetch API (或本地 Logo 库)
- [ ] 4.1.8 **Tombstones**: 快速插入交易清单模板 (Logo + 文字描述)

##### 4.2 Deck Check 与 Corporate Dictionary 联动

- [x] 4.2.1 扫描整个 PPT，检测字体是否偏离模板
- [x] 4.2.2 检测 Logo 位置偏移（支持 Logo 存在性检查、位置/尺寸阈值参数和 `LogoPositionIssues` 报告）
- [ ] 4.2.3 结合 Corporate Dictionary 高亮违禁词（DeckCheck 已支持术语检测、`forbiddenTerms` 参数和服务令牌方式自动读取 Backend `/api/dictionary/service-export`；幻灯片内高亮/批注待补）
- [x] 4.2.4 生成 PDF 合规报告（Sidecar 已输出品牌化 HTML/PDF 报告基础版，Web Deck Check 面板可触发导出，报告包含企业模板名称、状态摘要、总问题数、品牌色和 Logo 位置统计；完整品牌模板管理待补）

##### 4.3 形状对齐与布局工具

- [x] 4.3.1 对齐 (左/中/右/上/中/下)（Sidecar 命令、Backend 目录和 Ribbon 入口均已覆盖；算法已改为选中形状整体边界盒并补纯几何测试；逐命令真实行为验收另列）
- [x] 4.3.2 均匀分布 (水平/垂直)（Sidecar 命令、Backend 目录和 Ribbon 入口均已覆盖；算法已改为等边距分布并补纯几何测试；逐命令真实行为验收另列）
- [x] 4.3.3 统一尺寸 (宽度/高度)（Sidecar 命令、Backend 目录和 Ribbon 入口均已覆盖；真实 Ribbon 点击回归另列）
- [ ] 4.3.4 旋转与交换位置

##### 4.4 Reformat View (格式查找与替换)

- [ ] 4.4.1 按字体名称/大小/颜色搜索形状
- [ ] 4.4.2 按填充颜色/线条样式搜索
- [ ] 4.4.3 批量替换为指定格式

##### 4.5 Word 核心功能（部分已实现：DocBuilder、LinkToExcel）

- [ ] 4.5.1 导航窗格 (标题大纲)
- [x] 4.5.2 Doc Builder 问卷表单 + 文档生成（DD/CIM/Management Presentation 模板）
- [x] 4.5.3 Link to Excel (OLE 链接，支持刷新)
- [ ] 4.5.4 表格快速编辑工具 (插入行列、公式求和)

#### 交付物

- PowerPoint Sidecar 基础版（Dynamic Agendas、Deck Check、形状对齐/分布/统一尺寸的 Sidecar 命令；Ribbon 已暴露当前支持命令全集）
- Word Sidecar 服务 (包含 Doc Builder)
- Corporate Dictionary 联动基础：AIWA 已自动调用，Deck Check 支持手工传入 forbiddenTerms；配置服务令牌后可自动读取 Backend 字典

#### 阶段门禁

- PowerPoint 可基于 Section 生成并更新 Dynamic Agendas。
- Deck Check 能输出可读报告，当前覆盖字体、术语、页码缺失、文字密度、Logo 存在性/位置偏移检查和品牌化 HTML/PDF 报告基础版；颜色规则、幻灯片内批注和集中品牌模板管理待补。
- Word Link to Excel 可嵌入并刷新 Word 字段链接基础版；统一后端 LinkMetadata 驱动刷新已接入，支持 `FieldN`、`InlineShapeN`、`TableN` 精准刷新并在目标不足时回退全量扫描。
- PPT/Word 与 Excel 链接已有统一后端 LinkMetadata 模型；Sidecar 已支持按 PowerPoint `SlideN/ShapeN|ChartN|Name` 和 Word 字段/内联对象/表格目标定位刷新。

### 阶段五：整合与部署 (预计周期 2-3 周)

#### 目标

Sidecar + Web Add-in + 后端深度整合，性能优化，企业级部署完善。

#### 任务清单

##### 5.1 Ribbon 融合深化

- [ ] 5.1.1 统一命令调度: 所有按钮最终调用后端 API，后端再分发至 Sidecar 或 Web Add-in
- [ ] 5.1.2 动态菜单项 (根据上下文显示/隐藏)
- [ ] 5.1.3 快捷键与 Ribbon 按钮联动

##### 5.2 企业级功能增强

- [ ] 5.2.1 SSO 集成 (Azure AD / Okta)
- [ ] 5.2.2 用户组管理 (Admin Console 中可分配组，插件根据组显示功能)
- [ ] 5.2.3 软件降级权限 (支持强制回滚到旧版本)
- [x] 5.2.4 遥测关闭开关 (隐私合规)（`TelemetryEnabled=false` 默认关闭普通信息级使用统计；安全/认证/管理和 Warning+ 事件仍记录）
- [x] 5.2.5 Sidecar 本地 API 令牌保护（`Sidecar:LocalApiToken` 可选启用，保护 `/api/*`；Web task pane 与 Ribbon function-file 支持 `X-ModelForge-Sidecar-Token`；`/health` 保持公开）

##### 5.3 性能优化

- [ ] 5.3.1 Sidecar 启动时间优化 (推迟非关键初始化)
- [ ] 5.3.2 Web Add-in 懒加载 (按需加载组件)
- [ ] 5.3.3 内存占用优化 (避免 COM 对象泄漏)

##### 5.4 安全与合规

- [ ] 5.4.1 支持私有化 AI 部署 (对接 Llama3 等)
- [ ] 5.4.2 审计日志完整性保护（保留策略基础版已完成；防篡改链、归档签名和长期留存证明待补）
- [x] 5.4.3 提供安全自查清单文档（`docs/安全自查清单.md` 已补，生产 SSO/令牌/策略加固仍按独立任务推进）

##### 5.5 测试与文档

- [ ] 5.5.1 集成测试 (所有模块端到端)（Backend + Sidecar HTTP E2E 基础版、真实 Office COM 分项测试已覆盖 Excel/PPT/Word；Office 2024 用户式 E2E 已通过 Excel IFERROR、PPT 对齐、Excel→PPT 链接、Word 模板生成；Office manifest/function-file 已有 jsdom 静态回归；管理员 MSI 安装后双服务和 `/health` 回归已通过；Ribbon 侧载已在 Excel/Word/PowerPoint 显示并完成首轮点击巡检，仍待逐命令行为验收归档）
- [ ] 5.5.2 用户手册 + 视频教程（`docs/用户指南.md` 基础版已更新；视频教程待补）
- [x] 5.5.3 部署与运维文档 (Admin 手册)

#### 交付物

- Alpha 基础版：核心本地 Sidecar + Backend + Web Add-in 可运行
- 自动化测试和 COM 集成测试结果已记录在项目文档
- MSI/WiX 安装包可复现构建；管理员真机安装/卸载回归已通过并归档日志
- 用户式 Office 2024 E2E 脚本已归档：`scripts/manual-office-e2e.ps1`，产物输出到 `artifacts/manual-e2e/`
- 管理员文档、用户指南、安全自查清单和 Office Add-in 企业分发指南基础版已完成；视频教程待补

#### 阶段门禁

- 目标链路：Excel 建模 → PPT 链接 → Deck Check → 安全导出；当前已覆盖 HTTP E2E、Office COM 分项测试、Office 2024 用户式核心链路、Office manifest/function-file 静态回归、MSI 安装后服务链路回归和 Ribbon 真实侧载首轮巡检，仍需逐命令真实行为验收与 Deck Check→导出 PDF 用户链路回归。
- Sidecar 冷启动新增耗时控制在 1.5 秒以内。
- 关键 P0/P1 Sidecar 命令路径、Backend 认证和管理写操作已有基础审计事件；审计保留策略 dry-run/清理基础版已完成；Web UI 显式点击级 `ui.*` 审计基础版已完成，更细粒度输入变更/停留分析待补。
- 用户手册、管理员手册、部署手册、Office Add-in 企业分发指南和安全自查清单基础版齐备；视频教程待补。

## 4. 资源估算

| 阶段 | 功能模块               | 预计周期 | 优先级 | 累计周期 |
| ---- | ---------------------- | -------- | ------ | -------- |
| 1    | 基础设施 + 后端桥接    | 2 周     | P0     | 2 周     |
| 2    | Sidecar + Web 核心功能 | 5 周     | P0     | 7 周     |
| 3    | Web Add-in + 后台      | 3 周     | P1     | 10 周    |
| 4    | PPT/Word Sidecar 功能  | 4 周     | P1     | 14 周    |
| 5    | 整合优化 + 企业级功能  | 3 周     | P2     | 17 周    |

## 5. 里程碑

| 里程碑 | 预计时间 (周) | 交付内容                                |
| ------ | ------------- | --------------------------------------- |
| M1     | 2             | 基础设施完成，Sidecar + Web Add-in 原型 |
| M2     | 7             | Sidecar Excel P0 基础功能可用           |
| M3     | 10            | 企业后台 + AIWA demo 交付               |
| M4     | 14            | PPT/Word Sidecar 核心功能基础版完成     |
| M5     | 17            | Alpha 基础版，核心链路内部测试通过      |
| M6     | 18+           | 正式版本发布 (取决于测试修复)           |

## 6. 风险与应对

| 风险类别              | 具体描述                                 | 缓解策略                                         |
| --------------------- | ---------------------------------------- | ------------------------------------------------ |
| **Office 版本兼容性** | 不同版本 Office COM 接口行为差异         | 覆盖 Office 2016/2019/365 测试，关键功能回退方案 |
| **Sidecar 部署权限**  | 用户可能无管理员权限安装 Windows Service | 提供用户级 Tray App 模式作为降级方案             |
| **快捷键冲突**        | 与其他全局热键软件冲突                   | 内置冲突检测 + 提醒用户自定义                    |
| **跨应用链接稳定性**  | Excel 与 PPT 链接断裂难以追踪            | 增加链接健康状态检查，自动修复向导               |
| **AI 数据安全**       | 客户不允许将内容发送至公有 LLM           | 提供私有化部署选项 (Llama3)，支持 BYOK           |
| **后端桥接性能**      | 高频调用 API 可能延迟                    | 关键路径使用本地缓存，批量操作合并请求           |

## 7. 下一步行动

1. **完成 Ribbon 用户点击回归**：manifest/function-file 静态回归已覆盖 `FunctionFile`、`ExecuteFunction`、host 映射、可见反馈和 `ApiEnvelope<T>` 错误处理；Excel/PowerPoint/Word 已能侧载并显示 `ModelForge`，首轮点击巡检暴露的按钮无反馈、PPT 分布算法问题和 WPS 抢占 COM 问题已修复；下一步需先运行 `scripts/check-office-runtime.ps1 -StopWps`，再逐命令记录预期/实际行为、截图和 Sidecar 日志，确认 Task Pane、Sidecar REST 与 COM 操作一致。
2. **补齐剩余 P0/P1 产品化缺口**：100+ 快捷键对应命令规格、Deck Check 颜色规则/品牌模板管理、链接失效自动修复。
3. **推进企业级能力**：SSO/RBAC 细化、审计日志完整性保护/归档、策略配置和生产安全加固（审计保留策略与遥测关闭开关基础版已完成）。
4. **完善用户侧交付**：补视频教程和可复现演示脚本；用户手册、管理员手册、发布说明、Office Add-in 企业分发指南和安全自查清单基础版已完成。

## 8. MVP 切片计划

为避免一次性实现完整插件套件，建议将 MVP 拆成 4 个可演示切片：

| 切片                                  | 周期      | 目标                                                                          | 验收方式                                                                             |
| ------------------------------------- | --------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Slice 1：插件壳与桥接 ✅ 基础版       | 第 1-2 周 | Sidecar、Web、后端均可运行并互通                                              | Web/Sidecar/Backend HTTP 链路可达；Ribbon 到后端统一调度待产品化                     |
| Slice 2：Excel 建模效率 ✅ 基础版     | 第 3-4 周 | Power Tools、格式工具和快捷键框架可用                                         | 用样例模型演示预测行/列生成和公式封装；快捷键配置 UI 已完成基础版，100+ 全量映射待补 |
| Slice 3：模型审计与安全外发 ⚠️ 基础版 | 第 5-6 周 | Visualizations、Model Check 活动工作表基础扫描、Prepare to Share 部分能力可用 | 对含错误/外链/硬编码样例模型输出活动工作表审计结果；全工作簿审计和可选脱敏待补       |
| Slice 4：Excel-PPT 联动 ⚠️ 基础版     | 第 7 周   | Range/Chart 插入 PPT 原型可用                                                 | 后端元数据驱动刷新和单个对象精准定位基础版已完成；失效修复仍待补                     |

## 9. 工程治理与质量门禁

| 领域     | 门禁要求                                                                  |
| -------- | ------------------------------------------------------------------------- |
| 代码规范 | C# 使用 StyleCop / .editorconfig；TypeScript 使用 ESLint + Prettier。     |
| 测试     | 后端服务与共享逻辑必须有单元测试；Sidecar COM 逻辑尽量抽离到可测试类库。  |
| 文档     | 新增 API、环境变量、命令、配置项必须同步更新 `docs/`。                    |
| 安全     | 涉及文件删除、公式转值、隐私清理、AI 上传的能力必须有二次确认或策略控制。 |
| 可观测性 | 后端、Sidecar、本地日志统一 TraceId；支持导出诊断包。                     |
| 发布     | 每个阶段必须产出演示脚本、变更说明和已知问题清单。                        |

## 10. 测试策略

### 10.1 测试分层

1. **单元测试**：后端服务、命令解析、配置合并、字典规则、公式分析辅助逻辑。
2. **集成测试**：Sidecar 调用后端、Web 调用后端、链接元数据读写、审计事件上报。
3. **Office 手工回归**：Excel/PowerPoint/Word 真实宿主中的 Ribbon、COM 操作、文件保存与刷新；命令级用户式 E2E 可运行 `scripts/manual-office-e2e.ps1`，Ribbon 函数文件静态回归由 `src/web/src/__tests__/officeManifest.test.ts` 覆盖。
4. **兼容性测试**：Office 2016、Office 2019、Microsoft 365；32/64 位 Office 需明确支持策略。
5. **性能测试**：大工作簿扫描、跨应用刷新、冷启动耗时、内存泄漏和 COM 对象释放。

### 10.2 测试样例资产

- `samples/excel/financial-model-basic.xlsx`：基础三表模型。
- `samples/excel/model-with-errors.xlsx`：包含错误值、外链、循环引用、硬编码。
- `samples/powerpoint/investment-committee-template.pptx`：投委会材料模板。
- `samples/word/due-diligence-template.docx`：尽调文档模板。

> 样例文件不应包含真实客户数据；如需提交仓库，应使用合成数据。

## 11. 发布与部署策略

| 组件       | 开发期                              | 内测期                     | 企业部署期                                      |
| ---------- | ----------------------------------- | -------------------------- | ----------------------------------------------- |
| Sidecar    | dotnet run 本地控制台调试           | MSI 安装为 Windows Service | MSI/WiX 或企业软件分发平台                      |
| Web Add-in | Vite dev server + sideload manifest | 内部 HTTPS 静态站点        | 企业可信域名 + 管理中心分发                     |
| 后端 API   | 本地 ASP.NET Core + 开发数据库      | 单机或容器化部署           | 企业内网 / 私有云 / SaaS 多租户                 |
| 数据库     | InMemory / SQLite 可选              | PostgreSQL 测试库          | 默认 PostgreSQL；通过 Provider 抽象保留扩展空间 |

## 12. 关键架构决策记录（ADR）待建

| ADR     | 决策主题                                                                                            | 状态                 |
| ------- | --------------------------------------------------------------------------------------------------- | -------------------- |
| ADR-001 | Sidecar 使用 .NET 10 + 原生 COM，后端使用 ASP.NET Core 10                                           | 已确认               |
| ADR-002 | Sidecar 与 Web Add-in 只通过后端桥接 + localhost REST 通信                                          | 已固化               |
| ADR-003 | MVP 优先 Excel Sidecar，PowerPoint/Word 后置                                                        | 已固化               |
| ADR-004 | 数据库支持 PostgreSQL + SQLite（EF Core 统一抽象）                                                  | 已决策（2026-06-05） |
| ADR-005 | Sidecar/Backend 安装分发采用 MSI/WiX Windows Service（已可复现构建，管理员真机安装/卸载回归已通过） | 已决策               |
| ADR-006 | AIWA 生产能力是否进入 1.0                                                                           | 待决策               |

## 13. 近期两周行动清单

> **状态更新 (2026-06-06)**：原第 1/2 周行动清单已完成。当前近期工作转为“安装包真机回归 + 剩余产品化缺口”。

### 第 1 周

- [x] 创建 `src/`、`manifest/`、`tests/`、`samples/` 基础目录。
- [x] 创建 Sidecar REST 服务工程并完成原生 COM 互操作验证。
- [x] 升级 ASP.NET Core 后端到 .NET 10，提供 `/health`、`/api/version`、基础配置接口。
- [x] 完成 Web Add-in / React 项目，集成 Office.js 初始化。
- [x] 编写 `docs/API契约.md`，定义命令、配置、审计、链接元数据的基础 DTO。

### 第 2 周

- [x] 打通 Sidecar → 后端、Web → 后端、Web → Sidecar 三条调用链路。
- [x] 定义首批 20 个 Excel 高频命令和快捷键映射；当前已扩展到 39 个已实现 Excel/PPT/Word 命令默认映射。
- [ ] 建立日志、异常处理、TraceId 和诊断导出基础方案（`ILogger`、Backend 成功/业务错误信封、Sidecar `/api/execute` / `/api/status` 信封、Sidecar `/api/execute` 基础审计自动上报、TraceId 基础、Backend `/api/admin/diagnostics` 只读诊断摘要与 `/api/admin/diagnostics/bundle` 安全 JSON 诊断包已实现；Serilog、真实日志文件打包和全链路 TraceId 待补）。
- [x] 完成开发环境文档、运行脚本和基础 CI。
- [x] 输出阶段一演示脚本（`docs/PhaseA-Demo-Script.md`）和阶段二 Excel MVP 详细任务拆分。

### 当前滚动两周

- [x] 在管理员 PowerShell 中运行 `.\scripts\test-msi-admin.ps1` 完成 MSI 安装/卸载回归，并归档 `artifacts/manual-e2e/msi-install-admin.log` / `msi-uninstall-admin.log`（安装、双服务启动、Backend/Sidecar `/health`、卸载和服务移除通过；非管理员回归已归档 1925/1603 权限失败证据；相对 MSI 路径 1324 失败路径已归档）。
- [x] 完成 Office 2024 用户式命令级 E2E：Excel IFERROR、PPT 对齐、Excel→PPT 链接、Word 模板生成。
- [x] 修复本地开发全栈启动脚本：`scripts/dev-all.cmd` 已改为纯 ASCII，避免中文乱码导致 `cmd` 语法错误；已验证 Backend、Sidecar 和 Web Add-in 三端健康检查通过。
- [x] 清理旧 VSTO/COM 加载项残留：新增并执行 `scripts/clear-legacy-vsto-addins.ps1`，删除历史 `HKCU\Software\Microsoft\Office\Excel\Addins\ModelForge.Excel` 注册项，避免 Excel 启动时加载已删除的 `src/vsto/ModelForge.Excel`。
- [x] 修复 Office Ribbon 真实侧载不显示问题：manifest 已移除非法嵌套 `Group`，恢复合法 `CustomTab -> Group -> Control` 层级；新增侧载 TrustedCatalog 脚本 `scripts/setup-office-addin-sideload.ps1`，manifest/function-file 定向测试 8/8 通过。
- [x] 修复 manifest 校验缺口：所有 Ribbon `Supertip` 已补齐必需 `Description`，新增测试覆盖 `Title`/`Description` 完整性，manifest/function-file 定向测试提升到 9/9。
- [x] 完成 Office manifest/function-file 静态回归：Excel/PPT/Word host `FunctionFile`、Ribbon `ExecuteFunction`、host 映射、PPT 13 个支持命令全量 Ribbon 入口、Sidecar `ApiEnvelope<T>` 成功/失败路径。
- [x] 修复 Web 登录真实用户路径：LoginPage 已解包 Backend `ApiEnvelope<LoginResponse>` 并覆盖成功/失败/畸形响应测试。
- [x] 修复 Web 主界面用户路径：清理 App 可见乱码、恢复 Links 面板入口、命令面板改用后端目录完整 `excel.*` ID。
- [x] 清理 Web Admin/AIWA 用户路径：Admin Console 可读中文文案并兼容 `ApiEnvelope<T>`，AIWA Mock 和 Corporate Dictionary 结果文案恢复可读。
- [x] 统一 Admin/API 用户路径：Admin 用户端点返回 `ApiEnvelope<T>`，创建用户输入校验/重复用户 409/缺失用户 404 信封错误已覆盖，修复启停用户 active 状态响应，Auth/Admin DTO 已集中到 Contracts，`apiClient.login` 与 Admin 方法复用统一信封解包，并新增 Admin 用户生命周期 E2E。
- [x] 补齐 Admin 角色权限只读基础版：Backend `/api/admin/roles` 返回内置 RBAC 权限矩阵，Admin Console 增加“角色权限”页，覆盖 Backend/E2E/Web 回归。
- [x] 补齐 Admin 审计统计摘要基础版：Backend `/api/admin/audit-events/summary` 聚合最近事件类型/用户/宿主 Top 10，Admin Console 审计页展示摘要、趋势、热力图和点击下钻，审计列表/摘要/CSV 导出已支持同条件服务端筛选，覆盖 E2E/Web 回归。
- [x] 补齐 Admin 审计保留策略基础版：Backend `/api/admin/audit-events/retention` 支持 `dryRun` 预览和真实清理，默认 `AuditRetentionDays=90`，InMemory/SQLite/PostgreSQL 审计存储均支持按 cutoff 删除，Admin Console 审计页和诊断面板已接入，覆盖 Backend/E2E/Web 回归。
- [x] 补齐 Sidecar 执行审计自动上报基础版：`/api/execute` 成功上报 `command.executed`，输入校验失败上报 `command.validation_failed`，Office 未运行或 COM 异常上报 `command.failed`，由 Backend 遥测策略决定是否落库，覆盖 Backend + Sidecar HTTP E2E。
- [x] 补齐 Backend 管理写操作审计基础版：Admin 用户创建/启停、配置更新、Corporate Dictionary 术语新增/删除/导入会记录 `admin.*` 审计事件，并覆盖 E2E。
- [x] 补齐 Backend 认证审计基础版：登录成功记录 `auth.login.succeeded`，登录失败记录 `auth.login.failed`，并覆盖 E2E。
- [x] 固化 Office 运行时预检：新增 `scripts/check-office-runtime.ps1`，可检测/关闭 WPS/Kingsoft 进程、确认 Microsoft Office 2016+/Office 2024 进程路径，并校验 Sidecar `/api/status` 返回的 COM 版本；`scripts/dev-all.cmd` 和 `scripts/manual-office-e2e.ps1` 已接入该预检，避免 WPS 抢占 `Excel.Application` ROT 后造成 Ribbon 按钮“无反应/误失败”。
- [ ] 完成 Office manifest Ribbon 真实点击回归：Excel/PowerPoint/Word 已能侧载并完成首轮点击巡检；按钮无反馈、PPT 对齐/分布算法缺陷、WPS/旧 Excel COM 误绑定已修复并增加预检防回归；仍需在无 WPS、Microsoft Office 2024 `EXCEL.EXE/POWERPNT.EXE/WINWORD.EXE` 环境下逐个核心按钮复测并归档截图/日志。
- [x] 完成快捷键配置界面和导入/导出（Sidecar `/api/shortcuts/export` / `/api/shortcuts/import` 与 Web Excel 面板基础 UI 已完成；100+ 快捷键产品化目标仍列为单独缺口）。
- [x] 完成 Corporate Dictionary 前端管理基础版，并接入 Backend 检查 API（术语增删、正则/替换规则、样例检查、命中高亮 UI、JSON 批量导入/导出和 CSV/XLSX 模板导入/导出已完成）。
- [x] 将 Corporate Dictionary 进一步接入 AIWA 生成结果后处理。
- [x] 完成链接元数据驱动刷新基础版：`excel.refresh-links` 读取 Backend `/api/links`，按 PowerPoint/Word 目标宿主规划刷新，并保留后端不可达/无元数据时的本机全量刷新回退。
- [x] 完成 Admin 诊断摘要基础版：`/api/admin/diagnostics` 输出版本、数据库连接、命令/链接/字典/审计计数、审计保留策略和默认配置摘要，Web Admin Console 已提供“诊断”面板和安全 JSON 诊断包下载，并排除密钥、认证令牌、工作簿内容和本机日志文件。
- [x] 补齐 PPT 完整形状对齐/分布能力（Sidecar 命令、Backend 目录和 Office Ribbon 用户入口均已覆盖 13 个支持命令；对齐/分布已改为确定性几何算法并补纯算法测试；逐命令真实行为验收仍归入单独回归项）。
- [x] 补齐 PPT Deck Check PDF 报告和 Logo 检查基础版（品牌化 HTML/PDF 报告、Logo 存在性、Logo 位置偏移、企业模板名称、状态摘要、总问题数和品牌色输出已完成；集中品牌模板管理待补）。
- [x] 补充用户手册、安全自查清单和 Office Add-in 企业分发指南（基础版已完成；视频教程/截图化操作手册待补）。

